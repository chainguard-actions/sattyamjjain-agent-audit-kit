# Scan Target Fixture

This directory is used as a scan target for AgentAuditKit tests.
It contains no MCP configuration files or agent-related content,
so the scanner should find zero findings here.
