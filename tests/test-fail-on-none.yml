name: Test fail-on none (never fail)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up a git origin reachable inside the Docker container
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"
          rm -rf "$GITHUB_WORKSPACE/.git-remote"
          git init --bare "$GITHUB_WORKSPACE/.git-remote"
          git push "$GITHUB_WORKSPACE/.git-remote" HEAD:refs/heads/act-harness
          git remote set-url origin "$GITHUB_WORKSPACE/.git-remote" 2>/dev/null || \
            git remote add origin "$GITHUB_WORKSPACE/.git-remote"

      - id: scan
        uses: .
        with:
          path: '.'
          severity: 'low'
          fail-on: 'none'
          format: 'sarif'
          upload-sarif: 'false'
          comment-on-pr: 'false'

      - name: Assert exit-code is 0 when fail-on is none
        env:
          EXIT_CODE: ${{ steps.scan.outputs.exit-code }}
        run: |
          echo "exit-code: $EXIT_CODE"
          if [ "$EXIT_CODE" != "0" ]; then
            echo "FAIL: expected exit-code=0 when fail-on=none, got $EXIT_CODE"
            exit 1
          fi
          echo "PASS: exit-code=0 as expected with fail-on=none"

      - name: Assert all count outputs are numeric
        env:
          FINDINGS_COUNT: ${{ steps.scan.outputs.findings-count }}
          CRITICAL_COUNT: ${{ steps.scan.outputs.critical-count }}
          HIGH_COUNT: ${{ steps.scan.outputs.high-count }}
        run: |
          echo "findings-count: $FINDINGS_COUNT"
          echo "critical-count: $CRITICAL_COUNT"
          echo "high-count: $HIGH_COUNT"
          for val in "$FINDINGS_COUNT" "$CRITICAL_COUNT" "$HIGH_COUNT"; do
            if ! echo "$val" | grep -qE '^[0-9]+$'; then
              echo "FAIL: count output is not numeric: $val"
              exit 1
            fi
          done
          echo "PASS: all count outputs are numeric"
